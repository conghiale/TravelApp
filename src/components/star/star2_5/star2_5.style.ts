import { StyleSheet } from "react-native";
import Star1 from "./Star2_5";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignContent: 'center',
    },
})
 export default styles