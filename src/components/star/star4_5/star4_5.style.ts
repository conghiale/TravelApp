import { StyleSheet } from "react-native";
import Star1 from "./Star4_5";

const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignContent: 'center',
    },
    star: {
        marginHorizontal: 2,
    }
})
 export default styles